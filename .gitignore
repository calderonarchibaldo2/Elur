node_modules/
target/
dist/
build/
.DS_Store

# secrets / keys — NEVER publish (backstop; these are also excluded from the sync)
.env
*.env
!*.env.example
*-key.json
.agent-key.json
counterparty-key.json
*-secret.txt
*.txt.yale
*.owner.json
*.pem
id_ed25519*
id_rsa*

# editor / OS junk
.fuse_hidden*
*.swp

# Cloudflare local cache (leaks account email)
.wrangler/
wrangler-account.json

# superseded by the full web app build + web-demo
web-viewer/
